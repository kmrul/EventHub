@model IEnumerable<EventHub.Models.Event>
@{
    ViewBag.Title = "Mine";
}

<h2>My Upcomming Events</h2>


<ul class="events voffset4">
    @foreach (var evnt in Model)
    {
        <li class="event-list">
            <div class="date">
                <div class="month">
                    @evnt.DateTime.ToString("MMM")
                </div>
                <div class="day">
                    @evnt.DateTime.ToString("d ")
                </div>
            </div>
            <div class="details">
                <div class="name">
                    @evnt.Name
                </div>
                <div class="organizer">
                    @evnt.Vanue

                </div>
                <div class="category">
                    @evnt.Category.Name
                </div>

                <div class="my-actions">
                    <a href="@Url.Action("edit","events",new { id=evnt.Id})">Edit</a>
                    <a href="@Url.Action("mine","events")">Delete</a>
                </div>

            </div>

        </li>
    }
</ul>